fn not(b: Bool) -> Bool {
    if b { false } else { true }
}

fn or(a: Bool, b: Bool) -> Bool {
    if a { true } else { b }
}

fn and(a: Bool, b: Bool) -> Bool {
    if a { b } else { false }
}

fn xor(a: Bool, b: Bool) -> Bool {
    if a { not(b) } else { b }
}
