import std.io.print
import std.clock.now

fn main() -> Int uses { io, clock } {
    let start = now()
    -- `print` is generic over `Show` (Core Prelude, spec 0020/0021), so the Int
    -- is converted and printed directly; no `to_string` import is needed.
    print(start)
    start
}
