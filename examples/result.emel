struct Error {
  code: I32
}

enum Result {
  Ok(I32)
  Err(Error)
}

fn checked(value: I32) -> Result {
  match value == 0 {
    true -> Err(Error { code: 7 })
    false -> Ok(value)
  }
}

fn main() -> I32 {
  match checked(0) {
    Ok(value) -> value
    Err(error) -> error.code
  }
}
